apiVersion: v1
kind: Service
metadata:
  name: demo-transfer-dashboard
  namespace: cobra
  annotations:
    argocd.argoproj.io/sync-wave: "42"
spec:
  selector:
    app: demo-transfer-dashboard
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 3000
  type: ClusterIP
