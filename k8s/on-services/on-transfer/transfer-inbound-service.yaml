apiVersion: v1
kind: Service
metadata:
  namespace: cobra
  name: transfer-inbound-on-svc
  annotations:
    argocd.argoproj.io/sync-wave: "40"
  labels:
    app: transfer-inbound-on
spec:
  selector:
    app: transfer-inbound-on
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
