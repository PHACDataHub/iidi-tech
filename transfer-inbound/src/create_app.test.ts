import { create_app } from './create_app.ts';

describe('create_app', () => {
  describe('TODO', () => {
    it('TODO', async () => {
      //const app = await create_app();

      expect(true).toEqual(true);
    });
  });
});
